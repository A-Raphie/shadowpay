# ShadowPay — Handoff

Current: Phase 0 scaffolding. Starter kit cloned to _starter_ref, scaffold copied to src/cairo/public. Next: rebrand layout + global styles to ShadowPay dark vault, wire env, verify dev build, then registry PR.

Run: npm install; cp .env.example .env.local (add Alchemy key); npm run dev
Deploy: Vercel with NEXT_PUBLIC_PROVIDER_URL; set repo Website field.

Deadlines: Registry any time before Aug 31 23:59 UTC. 3 txs + video + live demo must be in repo at deadline. Hub polls every ~30m.
